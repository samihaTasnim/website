---
import PageLayout from "@/layouts/Base";
import BlogPostPreview from "@/components/BlogPostPreview";
import SocialList from "@/components/SocialList";
import { sortMDByDate } from "@/util";
import type { Post } from "@/types";

const meta = {
	title: "home",
	description: "home for my writings",
};

const MAX_POSTS = 10;
const allPosts = await Astro.glob<Post>("./posts/*.md");
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS);
---

<PageLayout meta={meta}>
	<section>
		<p>I'm a Full stack web developer, sucker for knowledge and a ISFJ-T. I interned at two companies previously. I also worked full time building UI parts, fixing bugs and adding extra features at Ripple Community, I had to quit because I wanted fo focus on getting good grades but I still haven't finished my high school graduation <small class="italic">yet.</small> </p>
		<br>
		<p>Currently, I am looking into open source contribution, blogging and studying full time. I still appear occasionaly on discord servers to hang out. I keep a <a href="/resources" class="">collection of resources</a> I found over the time.</p>
		<br>
		<p>During my free time, I enjoy reading books and watching movies. I also consume knowledge about various things on a weekly basis based on my current topic of interest. You can see my collections of favourite quotes <a href="/quotes" class="">here</a>.</p>
		<SocialList />
	</section>
	<section aria-label="Blog post list" class="mt-16">
		<h2 class="mb-4 text-xl title">Posts</h2>
		<ul class="space-y-4 sm:space-y-2">
			{
				allPostsByDate.map((p) => (
					<li class="flex flex-wrap items-center gap-x-2 [&_q]:w-full">
						<BlogPostPreview post={p} as="h2" />
					</li>
				))
			}
		</ul>
	</section>
</PageLayout>
